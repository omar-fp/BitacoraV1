<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".reportes"
    android:padding="16dp"
    android:background="@color/fondo_pantalla">

    <ImageView
        android:id="@+id/ivLogoReportes"
        android:layout_width="79dp"
        android:layout_height="85dp"
        android:src="@drawable/logo"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/tvTituloReportes"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="8dp"
        android:layout_marginTop="44dp"
        android:text="TODOS LOS\nREPORTES"
        android:textColor="@color/texto_principal"
        android:textSize="22sp"
        android:textStyle="normal"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/ivLogoReportes" />

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rvTodosLosReportes"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:layout_marginTop="24dp"
        android:layout_marginBottom="16dp"
        app:layout_constraintBottom_toTopOf="@+id/btnAtrasReportes"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/tvTituloReportes"
        tools:listitem="@layout/activity_item_reporte" />

    <Button
        android:id="@+id/btnAtrasReportes"
        android:layout_width="wrap_content"
        android:layout_height="56dp"
        app:backgroundTint="@color/fondo_botones"
        android:paddingHorizontal="32dp"
        android:text="Atrás"
        android:textColor="@color/texto_principal"
        app:cornerRadius="28dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>