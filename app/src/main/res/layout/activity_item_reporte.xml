<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="8dp"
    tools:context=".item_reporte"
    android:padding="16dp">

    <TextView
        android:id="@+id/tvMesReporte"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginEnd="16dp"
        android:background="#D9D9D9"
        android:elevation="4dp"
        android:padding="12dp"
        android:text="MES - AÑO"
        android:textColor="#000000"
        android:textSize="16sp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toStartOf="@+id/btnDescargar"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/btnDescargar"
        android:layout_width="120dp"
        android:layout_height="0dp"
        android:background="#D9D9D9"
        android:elevation="4dp"
        android:gravity="center"
        android:text="descargar"
        android:textColor="#B03A2E"
        android:textSize="14sp"
        app:layout_constraintBottom_toBottomOf="@+id/tvMesReporte"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="@+id/tvMesReporte" />

</androidx.constraintlayout.widget.ConstraintLayout>